#include <stdio.h>

/*
 * Main - printing teh alphabet
 *
 * Return - completed with zero
 */

int main(void)
{
        char letter;
	char c;

        for (letter = 'a'; letter <= 'z'; letter++)
                putchar(letter);

        for (c = "A"; c <= "Z"; c++)
		putchar(c)

	putchar('\n');

        return (0);
}

